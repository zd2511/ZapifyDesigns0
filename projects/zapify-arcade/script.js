const toast=document.getElementById('toast');function notify(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.tt);window.tt=setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelectorAll('.score').forEach(b=>b.onclick=()=>notify('Challenge launched — +'+b.closest('article').dataset.score+' XP potential!'));document.getElementById('shuffle').onclick=()=>{const board=document.getElementById('board');[...board.children].sort(()=>Math.random()-.5).forEach((x,i)=>{x.querySelector('span').textContent=String(i+1).padStart(2,'0');board.appendChild(x)});notify('Leaderboard shuffled ✦')};document.getElementById('join').onclick=()=>notify('You are on the event list — demo registration complete.');document.getElementById('play').onclick=()=>notify('Quick game loaded — dodge, collect, survive!');let sec=2*3600+14*60+38;setInterval(()=>{sec=Math.max(0,sec-1);const h=String(Math.floor(sec/3600)).padStart(2,'0'),m=String(Math.floor(sec%3600/60)).padStart(2,'0'),s=String(sec%60).padStart(2,'0');document.getElementById('count').textContent=`${h}:${m}:${s}`},1000);document.getElementById('sound').onclick=e=>{e.currentTarget.textContent=e.currentTarget.textContent.includes('ON')?'SFX OFF':'SFX ON';notify('Sound preference updated')};
